import 'package:flutter/material.dart';

imgError({
  double? width,
  double? height,
  required String img,
}) =>
    Container(
      width: width,
      height: height,
      color: Colors.grey[300],
    );
